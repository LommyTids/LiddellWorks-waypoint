{
  "name": "waypoint-app",
  "private": true,
  "version": "1.0.0",
  "description": "Waypoint travel planner, self-hosted on liddellworks.com/WayPoint via a Cloudflare Worker + KV.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node test-security.mjs && node test-design-tokens.js",
    "test:security": "node test-security.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
